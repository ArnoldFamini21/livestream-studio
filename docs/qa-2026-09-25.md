# QA audit — September 25, 2026

Baseline: `a9ba6db8` (main). Scope: client, signaling server, media server, shared contracts, deployment scripts, dependency audit, production health, and local browser smoke tests.

## Fixed in this batch

| Area | Finding | Correction and evidence |
| --- | --- | --- |
| Recording edits | Real edited exports failed with `Unrecognized option '/filter_complex'` on the bundled FFmpeg. | Use the compatible filter-script option for program, isolated-camera, and audio exports. Real FFmpeg test checks kept duration and picture/sound synchronization. |
| Audio-only recordings | Export tried to create a video MP4 and failed with a server error. | Produce WAV and MP3 plus the manifest; retain audio when optional video-export stems are disabled. Real FFmpeg verifies playable, correctly trimmed audio outputs. |
| Recording responses | A successful HTTP response containing HTML, empty content, or null was returned as a valid export job. | Reject invalid response bodies with an actionable error; regression coverage added. |
| Broadcast cleanup | Delayed cleanup from the previous HLS writer could delete a restarted broadcast's files. | Give each writer its own directory and delete only that directory. Filesystem regression confirms the replacement survives cleanup. |
| Encoder recovery | HLS stayed attached to the retired encoder after automatic encoder restart. | Stop the old HLS writer and start one against the replacement encoder. Guard asynchronous setup against a retired encoder. |
| Restart playback | The playlist URL and segment names were reused, allowing stale playback after a restart. | Version the playlist URL and give segments writer-specific names; disable HTTP caching of live footage. |
| Long broadcasts | The file allowlist rejected segment numbers after 99,999. | Accept bounded longer sequence numbers while continuing to reject traversal/non-media paths. |
| Watch polling | Slow or hung requests overlapped every five seconds and were not cancelled when leaving the page. | One request at a time, a 30-second request timeout, and cleanup cancellation. Tests cover timeout recovery and late responses. |
| Watch status | A network failure could appear as “Not live yet.” | Show a connection-interrupted state and keep retrying, without inventing an offline status. |
| Room lookup | Failed room lookup left viewers indefinitely on “Connecting…”. | Bounded requests, explicit error state, and retry action. |
| Registration flow | Playback/status requests could start before registration settings loaded. | Wait for room details and completion of the registration form before attaching the player. |
| Room navigation | Registration, live, and error state could carry between room routes. | Key the watch component by room ID so room-specific state resets. |
| Player cleanup | Safari's loaded-metadata listener and delayed play promises survived teardown. | Remove the listener and ignore completion after teardown. |
| Unsupported playback | Browsers without HLS support displayed an unexplained black player. | Display a playback error and recovery action. |
| Release verification | A media-only deployment required the untouched signaling server to report the new commit, incorrectly failing a healthy release. | Use per-service commit expectations, preserving health checks for both services; regression coverage includes partial releases. |

## Verification

- Workspace test suites: 1,269 passing; one PostgreSQL integration test skipped because `STUDIO_TEST_DATABASE_URL` is not configured locally.
- Deployment-script tests: 18 passing.
- All workspace builds and TypeScript checks pass.
- Dependency audit: zero known vulnerabilities at the time of this audit.
- Real FFmpeg: edited video/audio stay synchronized; edited audio-only WAV/MP3 duration is correct.
- Browser checks in the Codex browser: studio creation; host prejoin and entry with devices off; scene save; local chat send; synthetic video upload/presentation; three-page PDF render, next-page control, and content-only presentation; watch waiting/not-found states; registration submission and transition to waiting.
- Canvas measured at 16:9 with the sidebar open, closed, and while presenting. This verifies framing in the tested viewport, not every device configuration.
- Production baseline and post-release health checks passed for frontend, signaling, media, rendering, and Cloudflare TURN configuration.
- Post-release production HTTP test uploaded a synthetic audio-only recording, trimmed it, and verified WAV, MP3, and manifest artifacts in the configured R2 bucket. The temporary upload session was removed afterward.

## Boundaries and follow-up coverage

- No public social-platform broadcast was started during this audit. A long-running live broadcast with guests on separate restrictive networks remains an end-to-end validation task.
- Native Safari playback, mobile devices, system screen-picker behavior, and multi-hour broadcast soak tests were not exercised in the Codex browser.
- Browser presentation checks verify local rendering and controls; they do not establish remote guest reception or recording fidelity for every source.
- The existing public watch endpoint treats the room link as an unlisted viewing capability. Registration remains lead collection in the web UI, not server-enforced private/paywalled viewing authorization. Private viewing would require a separate viewer-access design.
- The build still reports a large client chunk warning; this is a performance follow-up, not a build failure.

## Follow-up coverage, September 25 (PR #321)

The boundaries above were exercised in Chromium (Playwright) against local servers.

| Area | Method | Finding | Result |
| --- | --- | --- | --- |
| Guests on restrictive networks | Relay-only ICE through a local TURN server (coturn and a Node TURN server) | An offer arriving while the previous answer was still being applied counted as a collision and was dropped; the call never connected. When both sides made the first offer at once, the polite side's rollback left Chrome gathering no candidates for that connection, and an ICE restart did not revive it. | Collisions decided in queue order; the polite side lets the other side offer first (1.5 s fallback); a connection stuck in "new" restarts ICE. Relayed calls 15/15 (was 12/15). |
| Guest screen share | Same, plus unrelayed | `addTrack` could reuse the transceiver receiving the host's simulcast camera, which cannot send: the screen sent 0 bytes. | Dedicated send-only transceiver, reused across shares. Relayed 10/10 (was 1/3); unrelayed share/stop/share 3/3; three-person calls 3/3. |
| Watch page, unsupported codecs | Chromium without H.264/AAC | Unbounded recovery sent about 800 playlist and segment requests a minute to the media server. | Bounded recovery policy: 2 requests, then a clear message. |
| Watch page size | Build output | The waiting page loaded the full hls.js (582 kB). | hls.js light loads only when live: 8 kB page. |
| Safari | Safari branch exercised in Chromium | hls.js on Safari turns AirPlay off; native playback had no error handling. | Native HLS on Safari (AirPlay, lock screen); retries at 1, 2, 4, 8, then every 10 s. Real Safari still untested. |
| PostgreSQL | Real SQL against PostgreSQL 16 | Five stores were tested only against a fake database. A rescheduled studio kept its old sort position. | Real-SQL tests (`STUDIO_TEST_DATABASE_URL`); upsert refreshes the sort key. 201/201 server tests with PostgreSQL. |
| Phones | iPhone 13, Pixel 7, iPhone SE, and 280 px | At 320 px the home page scrolled sideways and the Team tab was cut off. Share screen did nothing on phones. | Tabs and top bar fit down to 280 px; Share screen hidden without `getDisplayMedia`; 44 px hero tap target. Phone audit 27/27. |
| Long broadcast | 15 minutes of real-time 720p30 program with a polling viewer | None. Memory 114 to 117 MB; 4 FFmpeg processes throughout; HLS folder steady at about 6.6 MB; playlist sequence advanced to 442 with no reset; no processes left after stop; folder removed. | Crashed-server leftovers are now swept at startup (writer folders holding only HLS files). Folders from the pre-#319 naming are not swept; Render starts each deploy on a fresh disk. |

Still untested: real Safari and iOS devices, cross-network calls through Cloudflare TURN, and broadcasts of several hours.
